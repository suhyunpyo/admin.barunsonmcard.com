<a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" ria-expanded="false">
    <img class="img-profile rounded-circle" src="~/img/undraw_profile.svg" />
    @if (User.Identity.IsAuthenticated )
    {
        <span class="ml-2 d-none d-lg-inline small"><b>@User.Identity.Name</b>님 안녕하세요</span>
    }
</a>
<!-- Dropdown - User Information -->
<div class="dropdown-menu dropdown-menu-right shadow animated--grow-in" aria-labelledby="userDropdown">
    <a class="dropdown-item" href="#none" data-toggle="modal" data-target="#logoutModal" style="color:#333;">
        <i class="fas fa-sign-out-alt fa-sm fa-fw mr-2 text-gray-400"></i>
        Logout
    </a>
</div>